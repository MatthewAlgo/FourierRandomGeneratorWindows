# FourierRandomGeneratorWindows
A project built using WinBGI, also known as the "graphics.h" library. It features two random fourier generators, representing the X/Y-axis values. Two other windows represent this variation as a line.
